export interface Flight {
  airline: string;
  origin: string;
  departureDate: string;
  arrivalDate: string;
  departureTime: string;
  arrivalTime: string;
  price: number;
  airlineImage: string;
}
